<h1>Listing E-BURGER deliveries</h1>

<table class="table table-bdeliveryed">
  <% Order.all.each do |order| %>
    <tr>
      
        <td><%= order.id %></td>
      <td><%= order.address %></td>
      <td><%= order.delivery_method %></td>
      
    </tr>
  <% end %>
</table>

 <%= link_to "Add burger order", :controller => "deliveries", :action => 'add_burger_order'%>
